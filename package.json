{
  "name": "rule-sdk-node",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "homepage": "https://github.com/Emad-salah/rule-sdk#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/Emad-salah/rule-sdk.git"
  },
  "files": [
    "./dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "build:publish": "tsc && yarn publish",
    "build:publish:major": "tsc && yarn version major && yarn publish",
    "build:publish:minor": "tsc && yarn version minor && yarn publish",
    "build:publish:patch": "tsc && yarn version patch && yarn publish",
    "build:publish:prerelease": "tsc && yarn version prerelease && yarn publish"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.9.0",
    "eslint-config-standard": "^14.1.0",
    "eslint-plugin-import": "^2.19.1",
    "eslint-plugin-node": "^11.0.0",
    "eslint-plugin-prettier": "^3.1.2",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1",
    "husky": "^4.2.3",
    "prettier": "^1.19.1",
    "tslint": "^6.1.3",
    "typedoc": "^0.22.7",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^8.5.4",
    "axios": "^0.19.0",
    "jsonwebtoken": "^8.5.1"
  }
}
